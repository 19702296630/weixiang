# ===================== Web 框架 =====================
fastapi>=0.110
uvicorn[standard]>=0.29

# ===================== 数据库 =====================
# SQLAlchemy 2.0（同步引擎）+ PyMySQL 驱动
SQLAlchemy>=2.0
PyMySQL>=1.1

# ===================== LLM =====================
# DeepSeek 为 OpenAI 兼容接口，复用 openai SDK（重试/超时/JSON mode）
openai>=1.30

# ===================== 配置与校验 =====================
pydantic>=2.6
pydantic-settings>=2.2

# ===================== 测试 =====================
pytest>=8.0
pytest-cov>=4.1
httpx>=0.27
